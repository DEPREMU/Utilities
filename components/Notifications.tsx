import * as TaskManager from "expo-task-manager";
import * as Notifications from "expo-notifications";
import { fetchPrices, loadData } from "../utils/globalVariables/utils";

TaskManager.defineTask("BACKGROUND_CRYPTO_CURRENCY", async () => {});
