{
  "name": "utilities-scripts",
  "version": "0.0.0",
  "private": true,
  "main": "update.ts",
  "author": "DEPREMU",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint:fix": "eslint . --fix",
    "before-commit": "yarn run lint:fix && yarn run type-check"
  },
  "dependencies": {
    "form-data": "^4.0.6"
  }
}
