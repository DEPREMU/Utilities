{
  "license": "ISC",
  "name": "utilities-types",
  "private": true,
  "version": "0.0.0",
  "main": "index.d.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint:fix": "eslint . --fix",
    "before-commit": "yarn run type-check && yarn run lint:fix"
  }
}
